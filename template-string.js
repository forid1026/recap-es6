const multiLine = `This is first line
this is second line
this is third line
this is fourth line`;
console.log(multiLine);

const count = 5;
const getValue = `Get value in string using carrot ${count}`;
console.log(getValue)
